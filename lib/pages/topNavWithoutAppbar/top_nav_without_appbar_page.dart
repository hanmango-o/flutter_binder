import 'package:flutter/material.dart';

class TopNavWithoutAppBarPage extends StatelessWidget {
  const TopNavWithoutAppBarPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
